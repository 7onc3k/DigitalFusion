---
import Layout from '../layouts/Layout.astro';
import ClientLogos from '../components/ClientLogos.astro';
import { projects } from '../data/projects.js';

import Hero from '../components/Hero.astro';
const latestProjects = projects.slice(0, 3);

---
<Layout title="DigitalFusion - Vaše digitální řešení">
    <Hero 
        title="DigitalFusion: Vaše digitální transformace začíná zde"
        subtitle="Inovativní řešení pro moderní podniky"
        ctaText="Kontaktujte nás"
    />
    <section id="about-preview" class="py-16">
        <h2 class="text-3xl font-bold text-center mb-8">O nás</h2>
        <div class="about-content flex flex-col md:flex-row justify-between items-center">
            <div class="about-text flex-1 md:pr-8 mb-4 md:mb-0 text-center md:text-left">
                <p>Jsme tým expertů na digitální technologie, připraveni posunout vaše podnikání do budoucnosti.</p>
                <a href="/o-nas" class="btn inline-block mt-4 px-6 py-2 bg-white text-black rounded hover:bg-gray-200 transition duration-300">Zjistěte více</a>
            </div>
            <div class="about-image flex-1">
                <img src="/cesta/k/obrazku.jpg" alt="O nás" class="w-full rounded-lg shadow-lg" />
            </div>
        </div>
    </section>
    <section id="services-preview" class="py-16">
        <h2 class="text-3xl font-bold text-center mb-8">Naše služby</h2>
        <ul class="services-list flex flex-col md:flex-row justify-between mt-8 space-y-4 md:space-y-0 md:space-x-4">
            <li class="flex-1 text-center p-4 border border-white rounded">
                <i class="fas fa-laptop-code text-4xl mb-4"></i>
                <h3 class="text-xl font-bold">Webový design</h3>
                <p class="mt-2">Moderní a responzivní webové stránky</p>
            </li>
            <li class="flex-1 text-center p-4 border border-white rounded">
                <i class="fas fa-mobile-alt text-4xl mb-4"></i>
                <h3 class="text-xl font-bold">Vývoj aplikací</h3>
                <p class="mt-2">Mobilní a webové aplikace na míru</p>
            </li>
            <li class="flex-1 text-center p-4 border border-white rounded">
                <i class="fas fa-chart-line text-4xl mb-4"></i>
                <h3 class="text-xl font-bold">Digitální marketing</h3>
                <p class="mt-2">Efektivní online marketingové strategie</p>
            </li>
        </ul>
        <div class="text-center mt-8">
            <a href="/sluzby" class="btn inline-block px-6 py-2 bg-white text-black rounded hover:bg-gray-200 transition duration-300">Všechny služby</a>
        </div>
    </section>
    <section id="latest-projects" class="py-16">
        <h2 class="text-3xl font-bold text-center mb-8">Nejnovější projekty</h2>
        <div class="project-grid grid grid-cols-1 md:grid-cols-3 gap-8">
            {latestProjects.map(project => (
                <div class="project-card border border-white rounded overflow-hidden flex flex-col justify-between" data-category={project.category}>
                    <div>
                        <img src={project.image} alt={project.title} class="w-full h-48 object-cover" />
                        <h2 class="text-xl font-bold p-4">{project.title}</h2>
                        <p class="px-4">{project.description}</p>
                        <div class="technologies flex flex-wrap p-4">
                            {project.technologies.map(tech => (
                                <span class="tech-tag bg-white text-black px-2 py-1 rounded mr-2 mb-2 text-sm">{tech}</span>
                            ))}
                        </div>
                    </div>
                    <a href={`/portfolio/${project.id}`} class="view-details block px-4 py-2 bg-white text-black text-center rounded mt-auto hover:bg-gray-200 transition-colors">Zobrazit detaily</a>
                </div>
            ))}
        </div>
    </section>
    <ClientLogos />
    <section id="contact-cta" class="py-16 text-center">
        <h2 class="text-3xl font-bold mb-4">Připraveni začít?</h2>
        <p class="mt-4 mb-6">Kontaktujte nás a společně vytvoříme vaše digitální řešení.</p>
        <a href="/kontakt" class="cta-button inline-block px-6 py-3 bg-white text-black rounded font-bold hover:bg-gray-200 transition duration-300">Začněte projekt</a>
    </section>
</Layout>
---
interface Props {
    href?: string;
    type?: 'button' | 'submit' | 'reset';
    variant?: 'primary' | 'secondary' | 'outline';
    size?: 'small' | 'medium' | 'large';
    class?: string;
    target?: string;
    rel?: string;
}
const {
    href,
    type = 'button',
    variant = 'primary',
    size = 'medium',
    class: className,
    target,
    rel,
    ...rest
} = Astro.props;
const baseStyles = "inline-block font-bold rounded-full transition-all duration-300 text-center text-white bg-white bg-opacity-20 hover:bg-opacity-30";
const sizeStyles = {
    small: "px-4 py-2 text-sm",
    medium: "px-6 py-3 text-base",
    large: "px-8 py-4 text-lg"
};
const classes = `${baseStyles} ${sizeStyles[size]} ${className}`;
const Element = href ? 'a' : 'button';
---
<Element
    href={href}
    type={Element === 'button' ? type : undefined}
    class={classes}
    target={target}
    rel={rel}
    {...rest}
>
    <slot />
</Element>